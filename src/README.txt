Добрый день, спасибо большое за задание очень интересно было его решать, надеюсь вам понравится мое решение.

В папке api я создал файл products.json, чтобы писать fetch запрос (не получилось преобразовать в JSON формат данные, к сожалению, 

в моих проектах данные приходили в формате JSON). Поэтому я реализовал два подхода получения данных: с помощью import и с помощью fetch-запроса.

Добавил иконку изоображения по умолчанию (на случай если нет картинки на сайте)