const cardsContainer = document.querySelector(".furniture-items");

const basketContainer = document.querySelector(".popup__basket-items");
const basketButton = document.querySelector(".navigate__button");
const basketPopupButton = document.querySelector(".popup__basket");

export { cardsContainer, basketContainer, basketButton, basketPopupButton };
